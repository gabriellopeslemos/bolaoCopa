{
  "name": "bolao-copa",
  "version": "0.1.0",
  "private": true,
  "description": "Bolão da Copa do Mundo - app de palpites entre amigos (Android + iOS)",
  "workspaces": [
    "packages/*",
    "functions"
  ],
  "scripts": {
    "test": "npm test --workspace @bolao/scoring",
    "test:rules": "firebase emulators:exec --only firestore --project demo-bolaocopa \"vitest run --config vitest.rules.config.ts\"",
    "build:scoring": "npm run build --workspace @bolao/scoring"
  },
  "license": "MIT",
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.14.0",
    "firebase-admin": "^14.0.0",
    "firebase-tools": "^15.20.0",
    "vitest": "^4.1.8"
  }
}
