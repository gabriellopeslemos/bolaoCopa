{
  "name": "@bolao/scoring",
  "version": "0.1.0",
  "description": "Lógica de pontuação do bolão (função pura compartilhada entre app e Cloud Functions)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
